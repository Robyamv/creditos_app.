name: app_creditos
description: Aplicación móvil para gestionar créditos de clientes
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Iconos estilo iOS
  cupertino_icons: ^1.0.2

  # Para abrir WhatsApp y otras apps desde la app
  url_launcher: ^6.1.7

  # Firebase (para guardar clientes y deudas en la nube si quieres)
  firebase_core: ^2.1.1
  cloud_firestore: ^4.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
